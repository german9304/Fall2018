#include <iostream>
#include <string>

void permutations(string s){
	 string str = s;
	 for(int i = 0;i< s.length(); i++){
      char temp = s[i];
		 for(int index = 0; index < s.length(); index++){
         
		 }
	 }
}
int main(){
	string a = "ABC";
	permutations(s);
	return 0;
}
